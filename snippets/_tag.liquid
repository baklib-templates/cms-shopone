{% if tag %}
  <a href="{{ tag.path }}"
    class="inline-block text-inherit px-1 py-0.5 border hover:bg-primary rounded"
    style="
      background-color: hsl({{ tag.color_hls[0] }}, {{ tag.color_hls[1] }}%, 90%);
      color: {{ tag.color }};
    "
  >#{{ tag.name }}</a>
{% endif %}
